// 3] Create a new array whose elements are in uppercase of words present in the original array.

let arr = ["apple", "banana", "mango", "grapes"];

let upperArr = arr.map(ele => ele.toUpperCase());

console.log(upperArr);
